name: Svelte Check

permissions:
  contents: read

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  prettier:
    runs-on: docker

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run Svelte Check
        run: npm ci && npm run check
